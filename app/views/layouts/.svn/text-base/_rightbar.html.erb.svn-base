<div id="rightbar">

    <div id="active_members">
        <h5> Active Members </h5>
        <% User.find_active.each{|a| %>
            <%= link_to_function  h(a.name.downcase).sub("'", "&rsquo"), "submitByUser('#{a.id}');" %>
        <% } %>
    </div>
</div>
